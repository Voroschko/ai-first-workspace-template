# Настройка автоматического обновления проекта

Этот проект можно автоматически обновлять из оригинального репозитория шаблона. Есть несколько способов настройки:

## 🚀 Способ 1: Ручное обновление через скрипт (Рекомендуется для начала)

### Windows (PowerShell)

1. **Настройте upstream репозиторий (один раз):**
   ```powershell
   .\scripts\update-from-template.ps1
   ```
   При первом запуске скрипт попросит ввести URL оригинального репозитория шаблона.

2. **Последующие обновления:**
   ```powershell
   .\scripts\update-from-template.ps1
   ```

3. **Дополнительные опции:**
   ```powershell
   # Автоматическое слияние без запросов
   .\scripts\update-from-template.ps1 -AutoMerge
   
   # Проверка без реальных изменений
   .\scripts\update-from-template.ps1 -DryRun
   
   # Указать URL напрямую
   .\scripts\update-from-template.ps1 -UpstreamUrl "https://github.com/username/template-repo.git"
   ```

### Linux/Mac (Bash)

1. **Сделайте скрипт исполняемым:**
   ```bash
   chmod +x scripts/update-from-template.sh
   ```

2. **Запустите скрипт:**
   ```bash
   bash scripts/update-from-template.sh
   ```

3. **С опциями:**
   ```bash
   # Автоматическое слияние
   bash scripts/update-from-template.sh --auto-merge
   
   # Проверка без изменений
   bash scripts/update-from-template.sh --dry-run
   
   # Указать URL
   bash scripts/update-from-template.sh --url "https://github.com/username/template-repo.git"
   ```

## 🤖 Способ 2: Автоматическое обновление через GitHub Actions

GitHub Actions будет автоматически проверять и применять обновления из шаблона.

### Настройка:

1. **Добавьте Secret в GitHub:**
   - Перейдите в Settings → Secrets and variables → Actions
   - Нажмите "New repository secret"
   - Name: `UPSTREAM_TEMPLATE_URL`
   - Value: URL оригинального репозитория шаблона (например: `https://github.com/username/ai-first-workspace-template.git`)

2. **Workflow уже настроен:**
   - Файл `.github/workflows/auto-update-from-template.yml` уже создан
   - Автоматически запускается каждый понедельник в 9:00 UTC
   - Можно запустить вручную через Actions → "Auto Update from Template" → "Run workflow"

3. **Как это работает:**
   - GitHub Actions проверяет обновления каждую неделю
   - Если есть изменения, автоматически сливает их в main ветку
   - Если есть конфликты, создает Pull Request для ручного разрешения

## 📋 Способ 3: Ручное обновление через Git команды

Если предпочитаете делать все вручную:

```bash
# 1. Добавить upstream (один раз)
git remote add upstream https://github.com/username/template-repo.git

# 2. Получить обновления
git fetch upstream

# 3. Слить изменения
git checkout main
git merge upstream/main

# 4. Разрешить конфликты если есть
# (отредактируйте файлы с конфликтами, затем:)
git add .
git commit

# 5. Загрузить на GitHub
git push origin main
```

## ⚙️ Конфигурация

После первого запуска скрипта создается файл `.git-upstream-config` с настройками:

```json
{
  "upstreamUrl": "https://github.com/username/template-repo.git",
  "lastUpdated": "2024-01-15 10:30:00"
}
```

Этот файл автоматически используется при следующих запусках.

## 🔄 Рекомендуемый workflow

### Еженедельное обновление:
1. Запустите скрипт обновления
2. Проверьте изменения: `git diff`
3. Протестируйте проект
4. Если все хорошо: `git push`
5. Если есть проблемы: разрешите конфликты или откатите изменения

### Перед обновлением:
- ✅ Закоммитьте все текущие изменения
- ✅ Создайте backup ветку: `git branch backup-$(date +%Y%m%d)`
- ✅ Убедитесь, что все тесты проходят

### После обновления:
- ✅ Проверьте изменения: `git log --oneline -10`
- ✅ Протестируйте проект
- ✅ Проверьте, что нет конфликтов
- ✅ Обновите документацию если нужно

## 🛠️ Решение проблем

### Проблема: "upstream remote already exists"
```bash
# Удалить старый upstream
git remote remove upstream

# Добавить заново
git remote add upstream <URL>
```

### Проблема: Конфликты при слиянии
1. Скрипт автоматически сохранит ваши изменения в stash
2. Разрешите конфликты вручную
3. После разрешения: `git add . && git commit`
4. Восстановите изменения: `git stash pop`

### Проблема: Скрипт не запускается (PowerShell)
```powershell
# Разрешить выполнение скриптов (один раз)
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Проблема: GitHub Actions не работает
- Проверьте, что Secret `UPSTREAM_TEMPLATE_URL` добавлен
- Проверьте логи в Actions → "Auto Update from Template"
- Убедитесь, что workflow файл находится в `.github/workflows/`

## 📝 Примечания

- **Безопасность**: Скрипты не перезаписывают ваши локальные изменения без подтверждения
- **Конфликты**: При конфликтах скрипт остановится и попросит разрешить их вручную
- **Backup**: Рекомендуется создавать backup перед обновлением
- **Тестирование**: Всегда тестируйте проект после обновления

## 🔗 Полезные ссылки

- [Git документация](https://git-scm.com/doc)
- [GitHub Actions документация](https://docs.github.com/en/actions)
- [Работа с upstream репозиториями](https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork)




















