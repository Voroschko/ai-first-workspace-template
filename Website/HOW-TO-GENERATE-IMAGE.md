# 🎨 Как сгенерировать изображение через MCP

## ✅ Ваш запрос

Вы хотите сгенерировать изображение для Hero секции с описанием:
> "Современный офис с профессионалами за ноутбуками. Светлый интерьер, много естественного света, растения. Минималистичный стиль, профессиональная атмосфера. Цветовая схема: фиолетовый, синий, градиенты."

## 🚀 Способ 1: Через чат Cursor (рекомендуется)

### Шаг 1: Откройте новый чат в Cursor

Нажмите `Ctrl+L` (или `Cmd+L` на Mac) для открытия нового чата в Cursor.

### Шаг 2: Напишите запрос на генерацию

Скопируйте и вставьте этот запрос:

```
@imagegen-mcp Сгенерируй изображение: "Современный офис с профессионалами за ноутбуками. Светлый интерьер, много естественного света, растения. Минималистичный стиль, профессиональная атмосфера. Цветовая схема: фиолетовый, синий, градиенты. Размер: 1920x1080, стиль: vivid"
```

Или просто:

```
Сгенерируй изображение для Hero секции Go Offer: "Современный офис с профессионалами за ноутбуками. Светлый интерьер, много естественного света, растения. Минималистичный стиль, профессиональная атмосфера. Цветовая схема: фиолетовый, синий, градиенты."
```

### Шаг 3: Cursor автоматически использует MCP

Cursor автоматически вызовет настроенный `imagegen-mcp` сервер и сгенерирует изображение.

### Шаг 4: Скачайте сгенерированное изображение

- Изображение появится в чате Cursor
- Нажмите на изображение, чтобы открыть
- Скачайте и сохраните в `Website/public/images/hero/hero-main.jpg`

## 🔧 Способ 2: Через API Route (если MCP не работает)

Если MCP не работает напрямую, можно использовать API endpoint:

### Создайте файл `Website/app/api/generate-image/route.ts`:

```typescript
import { NextRequest, NextResponse } from 'next/server';

export async function POST(request: NextRequest) {
  const { prompt, size = '1024x1024', style = 'vivid' } = await request.json();

  if (!process.env.OPENAI_API_KEY) {
    return NextResponse.json(
      { error: 'OPENAI_API_KEY не настроен' },
      { status: 500 }
    );
  }

  try {
    const response = await fetch('https://api.openai.com/v1/images/generations', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${process.env.OPENAI_API_KEY}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        model: 'dall-e-3',
        prompt: `${prompt}. Professional photography, high quality, modern office interior.`,
        n: 1,
        size: size === '1024x1024' ? '1024x1024' : '1792x1024',
        style: style,
      }),
    });

    if (!response.ok) {
      const error = await response.json();
      return NextResponse.json({ error }, { status: response.status });
    }

    const data = await response.json();
    return NextResponse.json(data);
  } catch (error) {
    return NextResponse.json(
      { error: 'Ошибка при генерации изображения' },
      { status: 500 }
    );
  }
}
```

### Используйте через curl или Postman:

```bash
curl -X POST http://localhost:3000/api/generate-image \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "Современный офис с профессионалами за ноутбуками. Светлый интерьер, много естественного света, растения. Минималистичный стиль, профессиональная атмосфера. Цветовая схема: фиолетовый, синий, градиенты.",
    "size": "1792x1024",
    "style": "vivid"
  }'
```

## 📝 Улучшенный промпт для лучшего результата

Для Hero секции используйте этот более детальный промпт:

```
Professional photography of a modern office space with professionals working on laptops. 
Bright interior with lots of natural light from large windows. Indoor plants in minimalist pots. 
Clean, minimalist design with purple and blue gradient accents throughout the space. 
Professional atmosphere, modern tech company vibe. 
High quality, 4K, cinematic lighting, shallow depth of field, vibrant colors.
```

## ✅ После генерации

1. **Скачайте изображение** из Cursor
2. **Сохраните в:** `Website/public/images/hero/hero-main.jpg`
3. **Обновите Hero компонент:**

```tsx
// Website/components/Hero.tsx
import Image from 'next/image'

// Добавьте в секцию:
<Image 
  src="/images/hero/hero-main.jpg"
  alt="Modern Office"
  fill
  className="object-cover opacity-20"
  priority
  style={{ zIndex: 0 }}
/>
```

## 🆘 Если не работает

1. **Проверьте переменную окружения:**
   ```powershell
   echo $env:OPENAI_API_KEY
   ```

2. **Перезапустите Cursor** - MCP серверы загружаются при запуске

3. **Проверьте настройки MCP:**
   - Settings → Features → Model Context Protocol
   - Убедитесь, что `imagegen-mcp` отображается и активен

4. **Используйте API endpoint** как альтернативу (см. выше)

---

**Готово!** Используйте Способ 1 через чат Cursor - это самый простой метод! 🎨
