# 🚀 Быстрый Старт: MCP для Генерации Изображений

Краткая инструкция по настройке MCP сервера для генерации изображений.

## ⚡ Быстрая Настройка (5 минут)

### Шаг 1: Получите API ключ OpenAI

1. Перейдите на https://platform.openai.com/api-keys
2. Войдите в аккаунт или создайте новый
3. Создайте новый API ключ
4. Скопируйте ключ (он начинается с `sk-`)

### Шаг 2: Установите DALL-E MCP

```bash
npm install -g dalle-mcp
```

Или используйте через npx (без глобальной установки):
```bash
npx -y dalle-mcp
```

### Шаг 3: Настройте переменные окружения

Создайте файл `Website/.env.local`:

```env
OPENAI_API_KEY=sk-ваш_ключ_здесь
```

**⚠️ Важно:** Файл `.env.local` уже добавлен в `.gitignore` и не будет закоммичен в Git.

### Шаг 4: Проверьте доступность MCP в Cursor

MCP серверы настраиваются через конфигурацию Cursor. Проверьте настройки:
- **Settings → Features → MCP**
- Убедитесь, что MCP включен

### Шаг 5: Используйте генерацию изображений

Теперь вы можете попросить Cursor сгенерировать изображения:

```
Сгенерируй изображение для Hero секции: 
"Современный офис с людьми, работающими за ноутбуками, 
современный стиль, профессиональная атмосфера"
```

---

## 📝 Примеры Промптов для Генерации

### Для Hero секции:
```
Современный офис с профессионалами, работающими за ноутбуками. 
Светлый интерьер, много естественного света, растения. 
Современный минималистичный стиль, профессиональная атмосфера.
```

### Для секции Services:
```
Абстрактная иллюстрация технологий и карьеры. 
Геометрические формы, градиенты фиолетового и синего цветов. 
Минималистичный дизайн, современный стиль.
```

### Для секции Team:
```
Дружная команда профессионалов в современном офисе. 
Люди работают вместе, общаются, обсуждают проекты. 
Профессиональная атмосфера, позитивная энергия.
```

---

## 🎨 Интеграция в Next.js

### Вариант 1: Генерация через API Route

Создайте `Website/app/api/generate-image/route.ts`:

```typescript
import { NextRequest, NextResponse } from 'next/server';

export async function POST(request: NextRequest) {
  const { prompt, size = '1024x1024', style = 'vivid' } = await request.json();

  if (!process.env.OPENAI_API_KEY) {
    return NextResponse.json(
      { error: 'OPENAI_API_KEY не настроен' },
      { status: 500 }
    );
  }

  try {
    const response = await fetch('https://api.openai.com/v1/images/generations', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${process.env.OPENAI_API_KEY}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        model: 'dall-e-3',
        prompt,
        n: 1,
        size,
        style,
      }),
    });

    if (!response.ok) {
      const error = await response.json();
      return NextResponse.json({ error }, { status: response.status });
    }

    const data = await response.json();
    return NextResponse.json(data);
  } catch (error) {
    return NextResponse.json(
      { error: 'Ошибка при генерации изображения' },
      { status: 500 }
    );
  }
}
```

### Вариант 2: Использование в компоненте

Создайте `Website/components/ImageGenerator.tsx`:

```typescript
'use client';

import { useState } from 'react';

export default function ImageGenerator() {
  const [prompt, setPrompt] = useState('');
  const [imageUrl, setImageUrl] = useState('');
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState('');

  const generateImage = async () => {
    setLoading(true);
    setError('');
    
    try {
      const response = await fetch('/api/generate-image', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ 
          prompt,
          size: '1024x1024',
          style: 'vivid'
        }),
      });

      if (!response.ok) {
        throw new Error('Ошибка генерации');
      }

      const data = await response.json();
      setImageUrl(data.data[0].url);
    } catch (err) {
      setError('Не удалось сгенерировать изображение');
      console.error(err);
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="space-y-4 p-6">
      <div className="space-y-2">
        <label className="block text-sm font-medium">
          Описание изображения
        </label>
        <textarea
          value={prompt}
          onChange={(e) => setPrompt(e.target.value)}
          placeholder="Опишите изображение, которое хотите создать..."
          className="w-full p-3 border rounded-md min-h-[100px]"
          rows={4}
        />
      </div>
      
      <button
        onClick={generateImage}
        disabled={loading || !prompt.trim()}
        className="px-6 py-2 bg-purple-500 text-white rounded-md hover:bg-purple-600 disabled:opacity-50 disabled:cursor-not-allowed"
      >
        {loading ? 'Генерация...' : 'Сгенерировать изображение'}
      </button>

      {error && (
        <div className="p-3 bg-red-50 text-red-600 rounded-md">
          {error}
        </div>
      )}

      {imageUrl && (
        <div className="mt-4">
          <img 
            src={imageUrl} 
            alt={prompt} 
            className="max-w-full rounded-lg shadow-lg"
          />
          <div className="mt-2 text-sm text-muted">
            <a 
              href={imageUrl} 
              download
              className="text-purple-500 hover:underline"
            >
              Скачать изображение
            </a>
          </div>
        </div>
      )}
    </div>
  );
}
```

---

## 💡 Рекомендации

1. **Кэшируйте изображения** - не генерируйте одно и то же изображение дважды
2. **Сохраняйте локально** - скачивайте сгенерированные изображения в `public/images/generated/`
3. **Оптимизируйте промпты** - более детальные промпты дают лучшие результаты
4. **Используйте стили** - для консистентности используйте одинаковые стили (vivid/natural)

---

## 🔍 Проверка Работы

После настройки проверьте:

```bash
# 1. Проверьте переменные окружения
echo $OPENAI_API_KEY

# 2. Запустите dev сервер
cd Website
npm run dev

# 3. Откройте http://localhost:3000/api/generate-image
# Должен вернуться JSON ответ (или ошибку, если не POST запрос)
```

---

## 📚 Подробная Документация

Смотрите полную документацию в [MCP-IMAGE-GENERATION.md](./MCP-IMAGE-GENERATION.md)

---

## 🆘 Решение Проблем

### Ошибка "OPENAI_API_KEY не настроен"
- Убедитесь, что файл `.env.local` существует в папке `Website/`
- Проверьте, что ключ правильно скопирован
- Перезапустите сервер разработки

### Ошибка 401 (Unauthorized)
- Проверьте, что API ключ правильный
- Убедитесь, что на счету OpenAI есть средства
- Проверьте лимиты использования API

### MCP сервер не работает
- Перезапустите Cursor
- Проверьте настройки MCP в Cursor
- Убедитесь, что Node.js установлен

---

**Удачи в создании изображений! 🎨**
