# Решение проблемы "выполнение сценариев отключено"

## 🔴 Проблема

Вы видите ошибку:
```
Невозможно загрузить файл ... так как выполнение сценариев отключено в этой системе
```

## ✅ Решение (быстрое)

### Шаг 1: Откройте PowerShell от имени администратора

1. Нажмите `Win + X` или правой кнопкой на кнопку "Пуск"
2. Выберите **"Windows PowerShell (Администратор)"** или **"Терминал (Администратор)"**
3. Подтвердите запрос UAC (если появится)

### Шаг 2: Разрешите выполнение скриптов

Выполните эту команду:

```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

Нажмите `Y` (Yes) когда появится запрос подтверждения.

### Шаг 3: Проверьте, что все работает

Закройте PowerShell от администратора и откройте **обычный** PowerShell (не от имени администратора).

Затем выполните:

```powershell
node --version
npm --version
```

Теперь должно работать! ✅

---

## 🔍 Альтернативные решения

### Решение 1: Временно для текущей сессии

Если не хотите менять глобальную политику, выполните в обычном PowerShell:

```powershell
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process
```

Это работает только для текущего окна PowerShell.

### Решение 2: Использовать Command Prompt (cmd) вместо PowerShell

Если проблемы продолжаются, используйте обычную командную строку:

1. Нажмите `Win + R`
2. Введите `cmd` и нажмите Enter
3. В командной строке выполните:

```cmd
cd C:\Users\xante\Desktop\ai-first-workspace-template-main\Website
npm install
npm run dev
```

### Решение 3: Проверить текущую политику

Чтобы увидеть текущую политику выполнения:

```powershell
Get-ExecutionPolicy
Get-ExecutionPolicy -List
```

---

## 📋 Что делает команда

`Set-ExecutionPolicy RemoteSigned -Scope CurrentUser`

- **RemoteSigned** - разрешает выполнение локальных скриптов и подписанных удаленных скриптов
- **Scope CurrentUser** - применяется только для вашего пользователя (безопасно)

Это безопасная настройка, которая разрешает npm работать.

---

## ⚠️ Важно

После изменения политики:
1. Закройте все окна PowerShell
2. Откройте **новый** PowerShell
3. Попробуйте снова выполнить команды npm

---

**После этого npm должен работать нормально!**

















