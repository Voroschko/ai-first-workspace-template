# Решение проблем с отображением страницы

## 🔴 Проблема: http://localhost:3000/ не отображает страницу

### Шаг 1: Проверить, запущен ли сервер

В терминале, где вы запустили `npm run dev`, должно быть видно:

```
✓ Ready in X.Xs
○ Local:        http://localhost:3000
```

**Если сервер не запущен:**
1. Откройте терминал в папке `Website`
2. Выполните:
   ```powershell
   npm run dev
   ```

### Шаг 2: Проверить, установлены ли зависимости

Убедитесь, что существует папка `node_modules` в папке `Website`.

**Если папки `node_modules` нет:**
```powershell
cd C:\Users\xante\Desktop\ai-first-workspace-template-main\Website
npm install
```

Затем запустите сервер:
```powershell
npm run dev
```

### Шаг 3: Проверить ошибки в терминале

Если сервер запущен, но страница не отображается, проверьте терминал на наличие ошибок.

**Частые ошибки:**

#### Ошибка: "Cannot find module '@/components/...'"

**Решение:** Проверьте `tsconfig.json` - там должен быть настроен path alias:

```json
"paths": {
  "@/*": ["./*"]
}
```

#### Ошибка: "Module not found"

**Решение:** 
1. Удалите папку `node_modules` и файл `package-lock.json`
2. Выполните:
   ```powershell
   npm install
   npm run dev
   ```

### Шаг 4: Проверить, не занят ли порт 3000

Возможно, порт 3000 уже используется другим приложением.

**Решение:** Запустите на другом порту:
```powershell
npm run dev -- -p 3001
```

Затем откройте: http://localhost:3001

### Шаг 5: Проверить консоль браузера

1. Откройте http://localhost:3000
2. Нажмите `F12` для открытия инструментов разработчика
3. Перейдите на вкладку "Console"
4. Проверьте наличие ошибок

### Шаг 6: Проверить Network в браузере

1. Откройте инструменты разработчика (`F12`)
2. Перейдите на вкладку "Network"
3. Обновите страницу (`F5`)
4. Проверьте, загружаются ли файлы

### Шаг 7: Очистить кэш и перезапустить

1. Остановите сервер (`Ctrl + C` в терминале)
2. Удалите папку `.next` (если есть):
   ```powershell
   Remove-Item -Recurse -Force .next
   ```
3. Перезапустите:
   ```powershell
   npm run dev
   ```

## ✅ Быстрая проверка

Выполните эти команды по порядку:

```powershell
# 1. Перейдите в папку проекта
cd C:\Users\xante\Desktop\ai-first-workspace-template-main\Website

# 2. Проверьте, что node_modules существует
Test-Path node_modules

# 3. Если не существует - установите зависимости
npm install

# 4. Удалите .next папку (если есть)
Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue

# 5. Запустите сервер
npm run dev
```

## 🔍 Диагностика

### Проверить версии

```powershell
node --version  # Должна быть 18+
npm --version   # Должна быть установлена
```

### Проверить структуру файлов

Убедитесь, что существуют все файлы:
- `app/page.tsx`
- `app/layout.tsx`
- `app/globals.css`
- `components/Header.tsx`
- `components/Hero.tsx`
- `components/Services.tsx`
- `components/Footer.tsx`

### Проверить логи

В терминале, где запущен `npm run dev`, ищите:
- Ошибки компиляции
- Предупреждения
- Сообщения о том, на каком порту запущен сервер

## 🆘 Если ничего не помогает

1. Создайте новый файл `app/test.tsx`:
   ```tsx
   export default function Test() {
     return <div>Test page works!</div>
   }
   ```

2. Откройте http://localhost:3000/test
3. Если это работает - проблема в основном файле
4. Если не работает - проблема в конфигурации Next.js

---

**Сообщите, какие ошибки вы видите в терминале или в консоли браузера - это поможет точнее определить проблему!**


















