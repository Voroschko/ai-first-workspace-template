# 🚀 Быстрый запуск сервера Gofer

## ⚠️ Если видите ошибку "ERR_CONNECTION_REFUSED"

Это значит, что сервер не запущен. Следуйте этим шагам:

## Шаг 1: Проверьте, установлен ли Node.js

Откройте PowerShell или Command Prompt и выполните:

```powershell
node --version
```

**Если вы видите версию** (например, `v20.11.0`):
✅ Node.js установлен - переходите к Шагу 2

**Если вы видите ошибку** "node не является внутренней или внешней командой":
❌ Node.js не установлен - переходите к "Установка Node.js" ниже

## Шаг 2: Установите зависимости

```powershell
cd C:\Users\xante\Desktop\ai-first-workspace-template-main\Website
npm install
```

Дождитесь завершения установки (может занять несколько минут).

## Шаг 3: Запустите сервер

```powershell
npm run dev
```

Вы должны увидеть:
```
▲ Next.js 14.x.x
- Local:        http://localhost:3000
✓ Ready in X.Xs
```

## Шаг 4: Откройте браузер

Перейдите по адресу: **http://localhost:3000**

---

## 📥 Установка Node.js (если не установлен)

### Способ 1: Через официальный сайт (рекомендуется)

1. Откройте браузер
2. Перейдите на: **https://nodejs.org/**
3. Нажмите большую зеленую кнопку **"Download Node.js (LTS)"**
4. Запустите скачанный файл (`.msi`)
5. Следуйте инструкциям установщика:
   - Нажимайте "Next"
   - **ВАЖНО:** Убедитесь, что выбрана опция "Add to PATH"
   - Нажмите "Install"
6. После установки **закройте и заново откройте** PowerShell/Command Prompt
7. Проверьте: `node --version` (должна появиться версия)

### Способ 2: Через winget (Windows 10/11)

```powershell
winget install OpenJS.NodeJS.LTS
```

После установки закройте и заново откройте терминал.

---

## 🔧 Решение проблем

### Проблема: "node не является внутренней или внешней командой"

**Решение:**
1. Перезапустите терминал полностью (закройте и откройте заново)
2. Если не помогло - переустановите Node.js и убедитесь, что выбрана опция "Add to PATH"

### Проблема: Порт 3000 занят

**Решение:** Запустите на другом порту:
```powershell
npm run dev -- -p 3001
```
Затем откройте: **http://localhost:3001**

### Проблема: Ошибки при npm install

**Решение:**
1. Удалите папку `node_modules` (если есть)
2. Удалите файл `package-lock.json`
3. Запустите `npm install` снова

---

## ✅ Быстрый чек-лист

- [ ] Node.js установлен (`node --version` показывает версию)
- [ ] npm установлен (`npm --version` показывает версию)
- [ ] Зависимости установлены (есть папка `node_modules`)
- [ ] Сервер запущен (`npm run dev` выполнен)
- [ ] В терминале видно "✓ Ready" и адрес "http://localhost:3000"
- [ ] Браузер открывает страницу без ошибок

---

**После успешного запуска вы увидите лендинг Go Offer на http://localhost:3000**












