# MCP Серверы для Генерации Изображений

Рекомендации по выбору и настройке MCP (Model Context Protocol) серверов для генерации изображений на сайте.

## 🎯 Рекомендуемые MCP Серверы

### 1. **DALL-E MCP (Garoth)** ⭐ Рекомендуется для начала
**GitHub:** https://github.com/Garoth/dalle-mcp

**Преимущества:**
- ✅ Простая настройка
- ✅ Поддержка DALL-E 2 и DALL-E 3
- ✅ Редактирование изображений
- ✅ Создание вариаций (для DALL-E 2)
- ✅ Хорошая документация

**Функционал:**
- `generate_image` - генерация изображений по текстовому промпту
- `edit_image` - редактирование существующих изображений
- `create_variation` - создание вариаций изображения

**Требования:**
- OpenAI API ключ
- Node.js

**Установка:**
```bash
npm install -g dalle-mcp
```

---

### 2. **imagegen-mcp (spartanz51)** ⭐ Лучший баланс возможностей
**Reddit:** https://www.reddit.com/r/AI_Agents/comments/1kc9cs5

**Преимущества:**
- ✅ Поддержка DALL-E 2, DALL-E 3, GPT-Image-1
- ✅ Text-to-image и Image-to-image
- ✅ Полный контроль размера, качества, стиля
- ✅ Простая интеграция с Cursor/VSCode

**Функционал:**
- `text-to-image` - генерация из текста
- `image-to-image` - преобразование изображений (с маской)

**Требования:**
- OpenAI API ключ
- MCP-совместимый клиент (Cursor, Claude Desktop)

---

### 3. **Stability AI MCP Server (tadasant)** ⭐ Для продвинутых задач
**MCP Directory:** https://mcpnow.io/en/server/stability-ai-tadasant-mcp-server-stability-ai

**Преимущества:**
- ✅ Stable Diffusion модели
- ✅ Расширенные возможности редактирования
- ✅ Upscaling, outpainting
- ✅ Удаление фона
- ✅ Поиск и замена в изображениях

**Функционал:**
- Генерация изображений
- Редактирование с продвинутыми инструментами
- Увеличение разрешения
- Удаление фона
- Замена элементов в изображении

**Требования:**
- Stability AI API ключ (платная модель по кредитам)

---

### 4. **AiImageMultiStyle (fal.ai-based)** ⭐ Для стилизованных изображений
**MCP Directory:** https://mcp.so/server/aiimagemultistyle/codecraftm

**Преимущества:**
- ✅ Множество стилей (аниме, фэнтези, художественные)
- ✅ Stable Diffusion backend
- ✅ Resizing и upscaling
- ✅ Встроенная NSFW детекция
- ✅ Различные соотношения сторон

**Функционал:**
- Text-to-image с выбором стиля
- Изменение размера изображений
- Увеличение разрешения
- Поиск и замена элементов

**Требования:**
- fal.ai API ключ или альтернативный backend

---

## 📊 Сравнительная Таблица

| Сервер | Модели | Цена | Сложность | Лучше для |
|--------|--------|------|-----------|-----------|
| **DALL-E MCP** | DALL-E 2/3 | OpenAI API | ⭐ Простая | Быстрый старт, качественные изображения |
| **imagegen-mcp** | DALL-E 2/3, GPT-Image-1 | OpenAI API | ⭐⭐ Средняя | Универсальность, баланс возможностей |
| **Stability AI** | Stable Diffusion | Кредиты | ⭐⭐⭐ Высокая | Профессиональная обработка, редактирование |
| **AiImageMultiStyle** | Stable Diffusion | fal.ai | ⭐⭐ Средняя | Стилизованные изображения, арт |

---

## 🚀 Рекомендации по Выбору

### Для вашего сайта Go Offer рекомендуется:

1. **Начните с DALL-E MCP** - самый простой в настройке и использовании
2. **Добавьте Stability AI** - если нужны расширенные возможности редактирования
3. **Используйте AiImageMultiStyle** - для создания стилизованных иллюстраций для секций услуг

---

## ⚙️ Настройка MCP Сервера

### Шаг 1: Установка DALL-E MCP (рекомендуется для начала)

1. Установите через npm:
```bash
npm install -g dalle-mcp
```

2. Настройте переменные окружения:
```bash
# Создайте .env файл в корне проекта
OPENAI_API_KEY=your_api_key_here
```

### Шаг 2: Конфигурация Cursor

Создайте файл `.cursor/mcp.json` (если его нет):

```json
{
  "mcpServers": {
    "dalle-mcp": {
      "command": "npx",
      "args": ["-y", "dalle-mcp"],
      "env": {
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
      }
    }
  }
}
```

### Шаг 3: Перезапуск Cursor

Перезапустите Cursor для применения настроек.

---

## 🎨 Примеры Использования

### Генерация изображения для Hero секции

```typescript
// Пример API endpoint для генерации изображений
// Website/app/api/generate-image/route.ts

export async function POST(request: Request) {
  const { prompt, size = '1024x1024', style = 'vivid' } = await request.json();
  
  // Здесь будет вызов MCP сервера через API
  // Пока используйте прямой вызов OpenAI API
  const response = await fetch('https://api.openai.com/v1/images/generations', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${process.env.OPENAI_API_KEY}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      model: 'dall-e-3',
      prompt,
      n: 1,
      size,
      style,
    }),
  });
  
  const data = await response.json();
  return Response.json(data);
}
```

### Использование в компонентах

```typescript
// Website/components/ImageGenerator.tsx
'use client';

import { useState } from 'react';

export default function ImageGenerator() {
  const [prompt, setPrompt] = useState('');
  const [imageUrl, setImageUrl] = useState('');
  const [loading, setLoading] = useState(false);

  const generateImage = async () => {
    setLoading(true);
    try {
      const response = await fetch('/api/generate-image', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ prompt, size: '1024x1024' }),
      });
      const data = await response.json();
      setImageUrl(data.data[0].url);
    } catch (error) {
      console.error('Error generating image:', error);
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="space-y-4">
      <input
        type="text"
        value={prompt}
        onChange={(e) => setPrompt(e.target.value)}
        placeholder="Опишите изображение..."
        className="w-full p-2 border rounded"
      />
      <button
        onClick={generateImage}
        disabled={loading}
        className="px-4 py-2 bg-blue-500 text-white rounded"
      >
        {loading ? 'Генерация...' : 'Сгенерировать'}
      </button>
      {imageUrl && (
        <img src={imageUrl} alt={prompt} className="max-w-full rounded" />
      )}
    </div>
  );
}
```

---

## 🔒 Безопасность

### ⚠️ Важные рекомендации:

1. **Никогда не храните API ключи в клиентском коде**
   - Используйте переменные окружения
   - Создавайте серверные API endpoints

2. **Ограничьте доступ к генерации изображений**
   - Добавьте rate limiting
   - Используйте middleware для проверки авторизации

3. **Кэшируйте сгенерированные изображения**
   - Сохраняйте изображения в `public/images/generated/`
   - Избегайте повторной генерации одинаковых изображений

---

## 📁 Структура Папок для Сгенерированных Изображений

```
Website/
└── public/
    └── images/
        ├── generated/
        │   ├── hero/
        │   ├── services/
        │   ├── team/
        │   └── pricing/
        ├── tariffs/
        └── team/
```

---

## 💰 Стоимость Использования

### OpenAI DALL-E:
- **DALL-E 3 Standard (1024x1024):** ~$0.040 за изображение
- **DALL-E 3 HD (1024x1024):** ~$0.080 за изображение
- **DALL-E 2:** ~$0.020 за изображение

### Stability AI:
- Зависит от модели и разрешения (кредитная система)

### Рекомендации:
- Кэшируйте изображения для повторного использования
- Генерируйте изображения на этапе сборки, а не в runtime
- Используйте генерацию только для уникального контента

---

## 🔗 Полезные Ссылки

- [MCP Specification](https://modelcontextprotocol.io/)
- [DALL-E MCP GitHub](https://github.com/Garoth/dalle-mcp)
- [MCP Server Directory](https://mcpnow.io/)
- [OpenAI API Documentation](https://platform.openai.com/docs/guides/images)
- [Stability AI API Docs](https://platform.stability.ai/docs)

---

## 🎯 Следующие Шаги

1. ✅ Выберите MCP сервер (рекомендуется начать с DALL-E MCP)
2. ✅ Настройте переменные окружения
3. ✅ Создайте API endpoint для генерации изображений
4. ✅ Интегрируйте в компоненты сайта
5. ✅ Настройте кэширование изображений
6. ✅ Добавьте оптимизацию изображений через Next.js Image

---

**Последнее обновление:** Январь 2025
