# Установка Node.js для проекта Go Offer

## 📋 Инструкция по установке Node.js

### Шаг 1: Скачать Node.js

1. Перейдите на официальный сайт: **https://nodejs.org/**
2. Вы увидите две версии:
   - **LTS** (Long Term Support) - рекомендуется для большинства пользователей
   - **Current** - последняя версия с новыми функциями
3. **Скачайте LTS версию** (рекомендуется)

### Шаг 2: Установить Node.js

1. Запустите скачанный установщик (например, `node-vXX.X.X-x64.msi`)
2. Следуйте инструкциям мастера установки:
   - Нажмите "Next" на экране приветствия
   - Примите условия лицензии
   - Выберите путь установки (можно оставить по умолчанию)
   - **Важно:** Убедитесь, что выбрана опция "Add to PATH" (добавить в PATH)
   - Нажмите "Install"
   - Дождитесь завершения установки

### Шаг 3: Проверить установку

Откройте новый терминал (важно - перезапустите терминал!) и выполните:

```powershell
node --version
```

Должна отобразиться версия Node.js, например: `v20.10.0`

```powershell
npm --version
```

Должна отобразиться версия npm, например: `10.2.3`

### Шаг 4: Установить зависимости проекта

После установки Node.js:

```powershell
cd C:\Users\xante\Desktop\ai-first-workspace-template-main\Website
npm install
```

### Шаг 5: Запустить проект

```powershell
npm run dev
```

## ✅ Требования

- **Node.js версия:** 18.0.0 или выше (рекомендуется LTS)
- **npm версия:** Поставляется вместе с Node.js

## 🔧 Решение проблем

### Проблема: "node не является внутренней или внешней командой"

**Решение:**
1. Перезапустите терминал после установки
2. Проверьте, что Node.js добавлен в PATH:
   - Откройте "Переменные среды" (Environment Variables)
   - В "Системные переменные" найдите "Path"
   - Убедитесь, что там есть путь к Node.js (обычно `C:\Program Files\nodejs\`)
3. Если пути нет - переустановите Node.js и убедитесь, что выбрана опция "Add to PATH"

### Проблема: Устаревшая версия Node.js

**Решение:**
1. Скачайте последнюю LTS версию с nodejs.org
2. Переустановите Node.js (старая версия будет заменена)

## 📚 Дополнительная информация

- **Официальный сайт:** https://nodejs.org/
- **Документация:** https://nodejs.org/docs/
- **npm документация:** https://docs.npmjs.com/

---

**После установки Node.js вернитесь к проекту и выполните `npm install` в папке Website/**



















