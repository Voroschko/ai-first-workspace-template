# Проверка сервера разработки

## ✅ Быстрая диагностика

### 1. Запущен ли сервер?

В терминале, где вы выполнили `npm run dev`, вы должны видеть:

```
> company-website@0.1.0 dev
> next dev

   ▲ Next.js 14.x.x
   - Local:        http://localhost:3000

 ✓ Ready in X.Xs
```

**Если этого нет:**
- Сервер не запущен
- Запустите: `npm run dev` в папке Website

### 2. Что вы видите в браузере?

#### A) Белая/пустая страница
→ Проверьте консоль браузера (F12 → Console)

#### B) Ошибка "This site can't be reached"
→ Сервер не запущен или не отвечает

#### C) Ошибка "ERR_CONNECTION_REFUSED"
→ Сервер не запущен

#### D) Страница загружается, но ничего не видно
→ Проверьте консоль на ошибки JavaScript

### 3. Проверить терминал на ошибки

Ищите в терминале сообщения:
- ❌ `Error:`
- ⚠️ `Warning:`
- 🔴 Красные строки

## 🔧 Решения

### Решение 1: Перезапустить сервер

1. Остановите сервер: Нажмите `Ctrl + C` в терминале
2. Очистите кэш:
   ```powershell
   Remove-Item -Recurse -Force .next
   ```
3. Запустите заново:
   ```powershell
   npm run dev
   ```

### Решение 2: Проверить порт

Возможно, порт 3000 занят. Попробуйте другой порт:

```powershell
npm run dev -- -p 3001
```

Затем откройте: http://localhost:3001

### Решение 3: Проверить консоль браузера

1. Откройте http://localhost:3000
2. Нажмите `F12`
3. Перейдите на вкладку "Console"
4. Скопируйте все ошибки и покажите их

### Решение 4: Проверить Network

1. Откройте инструменты разработчика (`F12`)
2. Вкладка "Network"
3. Обновите страницу (`F5`)
4. Проверьте, какие файлы не загружаются (красные)

## 📋 Чек-лист

- [ ] Сервер запущен (`npm run dev` выполнен)
- [ ] В терминале нет ошибок
- [ ] Порту 3000 не блокируется
- [ ] Браузер открывает http://localhost:3000
- [ ] В консоли браузера нет ошибок

## 🆘 Если ничего не помогает

Выполните эти команды:

```powershell
cd C:\Users\xante\Desktop\ai-first-workspace-template-main\Website

# Остановите сервер (Ctrl+C если запущен)

# Удалите кэш
Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force node_modules -ErrorAction SilentlyContinue
Remove-Item package-lock.json -ErrorAction SilentlyContinue

# Переустановите зависимости
npm install

# Запустите сервер
npm run dev
```

---

**Важно: Сообщите, что именно вы видите:**
1. Что в терминале (где запущен npm run dev)?
2. Что в браузере (белая страница, ошибка, что-то другое)?
3. Что в консоли браузера (F12 → Console)?

























