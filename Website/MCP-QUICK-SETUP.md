# ⚡ Быстрая настройка imagegen-mcp

## ✅ Уже сделано

1. ✅ Создан файл `.cursor/mcp.json` с конфигурацией imagegen-mcp
2. ✅ Настроена модель `gpt-image-1` для генерации изображений

## 🚀 Что нужно сделать сейчас

### 1. Установите переменную окружения OPENAI_API_KEY

**Вариант A: Через PowerShell (временная, для текущей сессии):**
```powershell
$env:OPENAI_API_KEY="sk-ваш_ключ_здесь"
```

**Вариант B: Постоянная установка (рекомендуется):**
```powershell
[System.Environment]::SetEnvironmentVariable('OPENAI_API_KEY', 'sk-ваш_ключ_здесь', 'User')
```

После этого **перезапустите PowerShell и Cursor**.

### 2. Перезапустите Cursor

Полностью закройте и откройте Cursor заново для загрузки MCP конфигурации.

### 3. Проверьте в Cursor

1. Откройте **Settings** (Ctrl+,)
2. Перейдите в **Features → Model Context Protocol**
3. Убедитесь, что `imagegen-mcp` отображается в списке

## 🎨 Использование

Теперь в чате Cursor вы можете запросить генерацию изображения:

```
Сгенерируй изображение для Hero секции: 
"Современный офис с профессионалами за ноутбуками, 
светлый интерьер, минималистичный стиль"
```

## 📚 Подробная документация

Смотрите полную инструкцию в [MCP-IMAGEGEN-SETUP.md](./MCP-IMAGEGEN-SETUP.md)

---

**Готово! 🎨**
