# Решение проблем с PowerShell и установкой Node.js

## Проблема: "Выполнение сценариев отключено"

Если вы видите ошибку при попытке скачать или установить файлы из-за отключенного выполнения сценариев, вот решения:

## 🔧 Решение 1: Разрешить выполнение скриптов (временно)

Откройте PowerShell **от имени администратора** и выполните:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

Затем нажмите `Y` (Yes) для подтверждения.

### Проверить текущую политику:

```powershell
Get-ExecutionPolicy
```

## 🔧 Решение 2: Установить Node.js вручную (без скриптов)

### Вариант A: Через веб-браузер

1. Откройте браузер (Chrome, Edge, Firefox)
2. Перейдите на **https://nodejs.org/**
3. Нажмите на кнопку **"LTS"** для скачивания
4. Файл скачается в папку "Загрузки"
5. Найдите файл `node-vXX.X.X-x64.msi` в папке "Загрузки"
6. Дважды кликните по файлу для установки

### Вариант B: Прямая ссылка для скачивания

Откройте эту ссылку в браузере:
- **LTS версия:** https://nodejs.org/dist/v20.11.0/node-v20.11.0-x64.msi
- Или перейдите на https://nodejs.org/ и скачайте последнюю LTS версию

### Вариант C: Через пакетный менеджер Windows (winget)

Если у вас Windows 10/11 с поддержкой winget:

```powershell
winget install OpenJS.NodeJS.LTS
```

## 🔧 Решение 3: Изменить политику выполнения только для одной сессии

Если не хотите менять глобальную политику:

```powershell
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process
```

Это работает только для текущей сессии PowerShell.

## ✅ После установки Node.js

1. **Закройте и откройте новый терминал** (важно!)
2. Проверьте установку:

```powershell
node --version
npm --version
```

3. Перейдите в папку проекта:

```powershell
cd C:\Users\xante\Desktop\ai-first-workspace-template-main\Website
```

4. Установите зависимости:

```powershell
npm install
```

## 🚨 Альтернатива: Использовать Command Prompt (cmd) вместо PowerShell

Если проблемы с PowerShell продолжаются:

1. Откройте **Command Prompt (cmd)** вместо PowerShell
2. Выполните те же команды:
   ```cmd
   node --version
   npm --version
   cd C:\Users\xante\Desktop\ai-first-workspace-template-main\Website
   npm install
   ```

## 📋 Проверка политики выполнения

Чтобы увидеть текущую политику для разных областей:

```powershell
Get-ExecutionPolicy -List
```

Типичные значения:
- **Restricted** - скрипты заблокированы (по умолчанию)
- **RemoteSigned** - локальные скрипты разрешены, удаленные требуют подписи
- **Unrestricted** - все скрипты разрешены (не рекомендуется)

## 🔒 Безопасность

Рекомендуемая политика для обычных пользователей:
```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

Это позволяет:
- ✅ Выполнять локальные скрипты
- ✅ Блокировать небезопасные удаленные скрипты
- ✅ Обеспечивает базовую защиту

---

**Важно:** Если вы скачиваете установщик Node.js через браузер, проблемы с политикой выполнения скриптов не должны влиять на установку. Установщик - это обычная программа, а не скрипт.


















