# 🚀 Инструкция по деплою на Vercel

## Способ 1: Деплой через веб-интерфейс Vercel (Рекомендуется)

### Шаг 1: Подготовка проекта

1. Убедитесь, что проект находится в Git репозитории:
   ```bash
   cd Website
   git status
   ```

2. Если проект еще не в Git, инициализируйте репозиторий:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

3. Создайте репозиторий на GitHub (если еще нет):
   - Перейдите на https://github.com/new
   - Создайте новый репозиторий
   - Следуйте инструкциям для подключения локального репозитория

### Шаг 2: Деплой на Vercel

1. **Зарегистрируйтесь/войдите на Vercel:**
   - Перейдите на https://vercel.com
   - Войдите через GitHub аккаунт (рекомендуется)

2. **Импортируйте проект:**
   - Нажмите "Add New..." → "Project"
   - Выберите ваш GitHub репозиторий
   - Если репозитория нет в списке, нажмите "Adjust GitHub App Permissions"

3. **Настройте проект:**
   - **Framework Preset:** Next.js (определится автоматически)
   - **Root Directory:** `Website` (если проект в подпапке) или оставьте пустым
   - **Build Command:** `npm run build` (по умолчанию)
   - **Output Directory:** `.next` (по умолчанию)
   - **Install Command:** `npm install` (по умолчанию)

4. **Environment Variables (если нужны):**
   - Если используются переменные окружения, добавьте их в разделе "Environment Variables"
   - Например: `NEXT_PUBLIC_API_URL`, `DATABASE_URL` и т.д.

5. **Деплой:**
   - Нажмите "Deploy"
   - Дождитесь завершения сборки (обычно 2-5 минут)
   - После успешного деплоя вы получите URL вида: `https://your-project.vercel.app`

### Шаг 3: Настройка домена (опционально)

1. В настройках проекта перейдите в "Settings" → "Domains"
2. Добавьте ваш домен (например, `go-offer.us`)
3. Следуйте инструкциям для настройки DNS записей

---

## Способ 2: Деплой через Vercel CLI

### Шаг 1: Установка Vercel CLI

```bash
npm install -g vercel
```

### Шаг 2: Вход в Vercel

```bash
vercel login
```

### Шаг 3: Деплой

```bash
cd Website
vercel
```

Следуйте инструкциям в терминале:
- Set up and deploy? **Y**
- Which scope? Выберите ваш аккаунт
- Link to existing project? **N** (для первого деплоя)
- What's your project's name? Введите название или нажмите Enter
- In which directory is your code located? `./` или `Website`
- Want to override the settings? **N** (по умолчанию)

### Шаг 4: Production деплой

После тестового деплоя, для production:

```bash
vercel --prod
```

---

## Способ 3: Автоматический деплой через GitHub

### Шаг 1: Подключение GitHub репозитория

1. На Vercel перейдите в "Settings" → "Git"
2. Подключите GitHub аккаунт (если еще не подключен)
3. Выберите репозиторий с вашим проектом

### Шаг 2: Автоматические деплои

После подключения:
- **Каждый push в `main`/`master`** → автоматический production деплой
- **Pull Requests** → автоматические preview деплои
- **Другие ветки** → preview деплои

---

## ⚙️ Настройки проекта для Vercel

### Важные моменты:

1. **Root Directory:**
   - Если проект в папке `Website`, укажите `Website` в настройках проекта на Vercel

2. **Build Settings:**
   - Build Command: `npm run build` (или `cd Website && npm run build` если root не Website)
   - Output Directory: `.next` (по умолчанию для Next.js)

3. **Node.js Version:**
   - Vercel автоматически определяет версию из `package.json`
   - Убедитесь, что указана совместимая версия (18+)

4. **Environment Variables:**
   - Добавьте в "Settings" → "Environment Variables"
   - Доступны для Production, Preview и Development

---

## 🔧 Проверка перед деплоем

### Локальная проверка сборки:

```bash
cd Website
npm install
npm run build
npm run start
```

Если сборка проходит успешно локально, она пройдет и на Vercel.

### Проверка файлов:

Убедитесь, что в `.gitignore` есть:
```
node_modules
.next
.vercel
.env*.local
```

---

## 🐛 Решение проблем

### Ошибка сборки:

1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что все зависимости в `package.json`
3. Проверьте версию Node.js (должна быть 18+)

### Ошибка "Module not found":

1. Проверьте, что все файлы закоммичены в Git
2. Убедитесь, что `node_modules` в `.gitignore`

### Ошибка с путями:

1. Проверьте настройку "Root Directory" в Vercel
2. Убедитесь, что `next.config.js` настроен правильно

### Проблемы с изображениями:

1. Проверьте, что изображения в папке `public/`
2. Используйте `next/image` для оптимизации

---

## 📝 Полезные команды Vercel CLI

```bash
# Просмотр деплоев
vercel ls

# Просмотр логов
vercel logs

# Удаление проекта
vercel remove

# Просмотр информации о проекте
vercel inspect

# Деплой с переменными окружения
vercel --env NEXT_PUBLIC_API_URL=https://api.example.com
```

---

## ✅ Чеклист перед деплоем

- [ ] Проект коммитится в Git
- [ ] `npm run build` проходит успешно локально
- [ ] Все зависимости указаны в `package.json`
- [ ] `.gitignore` настроен правильно
- [ ] Environment variables добавлены (если нужны)
- [ ] Домен настроен (если используется кастомный домен)
- [ ] `next.config.js` настроен правильно

---

## 🎉 После деплоя

1. Проверьте работу сайта по предоставленному URL
2. Настройте кастомный домен (если нужно)
3. Настройте автоматические деплои из GitHub
4. Добавьте аналитику (если нужно)

---

**Готово! Ваш сайт должен быть доступен на Vercel! 🚀**
