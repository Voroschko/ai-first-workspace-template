# ⚙️ Настройка imagegen-mcp

Инструкция по подключению и настройке imagegen-mcp для генерации изображений в Cursor.

## ✅ Что уже сделано

1. ✅ Создан конфигурационный файл `.cursor/mcp.json`
2. ✅ Настроен imagegen-mcp с моделью `gpt-image-1`

## 🔧 Настройка переменных окружения

### Вариант 1: Через .env.local (Рекомендуется)

Создайте файл `Website/.env.local`:

```env
OPENAI_API_KEY=sk-ваш_ключ_здесь
```

**Важно:** Этот файл уже в `.gitignore` и не будет закоммичен.

### Вариант 2: Через системные переменные окружения Windows

#### Временная установка (для текущей сессии PowerShell):

```powershell
$env:OPENAI_API_KEY="sk-ваш_ключ_здесь"
```

#### Постоянная установка (для пользователя):

```powershell
[System.Environment]::SetEnvironmentVariable('OPENAI_API_KEY', 'sk-ваш_ключ_здесь', 'User')
```

После установки **перезапустите PowerShell и Cursor**.

### Вариант 3: Прямо в mcp.json (не рекомендуется для production)

Если хотите указать ключ напрямую в конфигурации (⚠️ не безопасно для Git):

Отредактируйте `.cursor/mcp.json`:

```json
{
  "mcpServers": {
    "imagegen-mcp": {
      "command": "npx",
      "args": ["-y", "imagegen-mcp", "--models", "gpt-image-1"],
      "env": {
        "OPENAI_API_KEY": "sk-ваш_реальный_ключ_здесь"
      }
    }
  }
}
```

**⚠️ Внимание:** Не коммитьте файл с реальными ключами в Git!

---

## 🔍 Проверка настройки

### Шаг 1: Проверьте наличие файла конфигурации

```powershell
Test-Path .cursor\mcp.json
```

Должно вернуть `True`.

### Шаг 2: Проверьте переменную окружения

```powershell
echo $env:OPENAI_API_KEY
```

Должен показать ваш API ключ (если установлен).

### Шаг 3: Перезапустите Cursor

1. Полностью закройте Cursor
2. Откройте его снова
3. Cursor автоматически загрузит MCP конфигурацию

### Шаг 4: Проверьте MCP серверы в Cursor

1. Откройте **Settings** (Ctrl+,)
2. Перейдите в **Features → Model Context Protocol**
3. Должен отображаться `imagegen-mcp` в списке серверов
4. Проверьте статус подключения

---

## 🎨 Использование

После настройки вы можете использовать генерацию изображений прямо в чате Cursor:

```
Сгенерируй изображение: "Современный офис с профессионалами, 
работающими за ноутбуками, светлый интерьер, минималистичный стиль"
```

Или:

```
Создай изображение для Hero секции сайта: 
"Абстрактная иллюстрация технологий и карьеры, 
градиенты фиолетового и синего цветов"
```

Cursor автоматически:
- Использует MCP сервер `imagegen-mcp`
- Генерирует изображение через `gpt-image-1`
- Отображает результат в чате

---

## 🛠️ Альтернативные модели

Если хотите использовать другие модели, измените параметр `--models` в `.cursor/mcp.json`:

### Для DALL-E 3:
```json
"args": ["-y", "imagegen-mcp", "--models", "dall-e-3"]
```

### Для DALL-E 2:
```json
"args": ["-y", "imagegen-mcp", "--models", "dall-e-2"]
```

### Для нескольких моделей:
```json
"args": ["-y", "imagegen-mcp", "--models", "gpt-image-1,dall-e-3"]
```

---

## 🔧 Устранение неполадок

### Проблема: MCP сервер не появляется в Cursor

**Решение:**
1. Убедитесь, что файл `.cursor/mcp.json` существует
2. Проверьте синтаксис JSON (должен быть валидным)
3. Перезапустите Cursor
4. Проверьте настройки MCP в Cursor Settings

### Проблема: Ошибка "OPENAI_API_KEY not found"

**Решение:**
1. Установите переменную окружения (см. выше)
2. Перезапустите PowerShell и Cursor
3. Проверьте, что ключ правильный: `echo $env:OPENAI_API_KEY`

### Проблема: "npx command not found"

**Решение:**
1. Установите Node.js: https://nodejs.org/
2. Проверьте установку: `node --version` и `npm --version`
3. Перезапустите Cursor после установки Node.js

### Проблема: На Windows команда не выполняется

**Решение:**
Если на Windows возникают проблемы, попробуйте использовать `cmd /c`:

Отредактируйте `.cursor/mcp.json`:

```json
{
  "mcpServers": {
    "imagegen-mcp": {
      "command": "cmd",
      "args": [
        "/c",
        "npx",
        "-y",
        "imagegen-mcp",
        "--models",
        "gpt-image-1"
      ],
      "env": {
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
      }
    }
  }
}
```

### Проблема: MCP инструменты не работают

**Решение:**
1. Обновите Cursor до последней версии
2. Проверьте логи MCP в Cursor Settings → MCP → View Logs
3. Убедитесь, что у вас есть доступ к интернету (для загрузки пакетов через npx)

---

## 📝 Примеры конфигурации

### Полная конфигурация с несколькими моделями:

```json
{
  "mcpServers": {
    "imagegen-mcp": {
      "command": "npx",
      "args": [
        "-y",
        "imagegen-mcp",
        "--models",
        "gpt-image-1,dall-e-3"
      ],
      "env": {
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
      }
    }
  }
}
```

### Конфигурация для Windows с cmd:

```json
{
  "mcpServers": {
    "imagegen-mcp": {
      "command": "cmd",
      "args": [
        "/c",
        "npx",
        "-y",
        "imagegen-mcp",
        "--models",
        "gpt-image-1"
      ],
      "env": {
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
      }
    }
  }
}
```

---

## 🔗 Полезные ссылки

- [MCP Documentation](https://modelcontextprotocol.io/)
- [Cursor MCP Guide](https://docs.cursor.com/context/model-context-protocol)
- [OpenAI API Keys](https://platform.openai.com/api-keys)
- [imagegen-mcp GitHub](https://github.com/spartanz51/imagegen-mcp) (если есть)

---

## ✅ Чеклист настройки

- [ ] Создан файл `.cursor/mcp.json`
- [ ] Установлена переменная окружения `OPENAI_API_KEY`
- [ ] Установлен Node.js (для npx)
- [ ] Перезапущен Cursor
- [ ] Проверено наличие `imagegen-mcp` в Settings → MCP
- [ ] Протестирована генерация изображения в чате

---

**Готово! Теперь вы можете генерировать изображения прямо в Cursor! 🎨**
