# 🚀 Запуск проекта Gofer - Инструкция

## ❌ Проблема: ERR_CONNECTION_REFUSED

Вы видите ошибку "Connection Failed" потому что сервер не запущен. Вот как это исправить:

---

## 📋 Шаг 1: Проверьте установку Node.js

Откройте **PowerShell** или **Command Prompt** и выполните:

```powershell
node --version
```

### ✅ Если вы видите версию (например, `v20.11.0`)
Node.js установлен! Переходите к **Шагу 2**.

### ❌ Если видите ошибку "node не является внутренней или внешней командой"
**Node.js не установлен.** Следуйте инструкции ниже.

---

## 📥 Установка Node.js (если не установлен)

### Простой способ:

1. **Откройте браузер** (Chrome, Edge, Firefox)
2. Перейдите на: **https://nodejs.org/**
3. Нажмите большую зеленую кнопку **"Download Node.js (LTS)"**
4. Откройте скачанный файл (`.msi`)
5. Следуйте инструкциям:
   - Нажимайте "Next"
   - **ВАЖНО:** Убедитесь что выбрана опция **"Add to PATH"**
   - Нажмите "Install"
6. После установки **закройте и заново откройте PowerShell**
7. Проверьте снова: `node --version`

---

## 🚀 Шаг 2: Запустите проект

Откройте PowerShell в папке проекта и выполните:

```powershell
# Перейдите в папку Website
cd C:\Users\xante\Desktop\ai-first-workspace-template-main\Website

# Установите зависимости (если еще не установлены)
npm install

# Запустите сервер
npm run dev
```

### ✅ Что вы должны увидеть:

```
▲ Next.js 14.x.x
- Local:        http://localhost:3000

✓ Ready in X.Xs
```

---

## 🌐 Шаг 3: Откройте в браузере

Когда увидите "✓ Ready", откройте в браузере:

**http://localhost:3000**

---

## 🔧 Решение проблем

### Проблема: Порт 3000 занят

Запустите на другом порту:

```powershell
npm run dev -- -p 3001
```

Затем откройте: **http://localhost:3001**

### Проблема: Ошибки при npm install

Попробуйте очистить и переустановить:

```powershell
Remove-Item -Recurse -Force node_modules -ErrorAction SilentlyContinue
Remove-Item package-lock.json -ErrorAction SilentlyContinue
npm install
```

### Проблема: Сервер запущен, но страница не открывается

1. Убедитесь, что в терминале видно "✓ Ready"
2. Проверьте адрес: должен быть `http://localhost:3000` (не `https://`)
3. Попробуйте другой браузер
4. Откройте консоль браузера (F12) и проверьте ошибки

---

## 📝 Быстрый чек-лист

- [ ] Node.js установлен (`node --version` показывает версию)
- [ ] npm установлен (`npm --version` показывает версию)  
- [ ] Перешел в папку `Website`
- [ ] Выполнил `npm install`
- [ ] Запустил `npm run dev`
- [ ] Вижу "✓ Ready" в терминале
- [ ] Открыл http://localhost:3000 в браузере

---

## 🆘 Нужна помощь?

Если что-то не работает, сообщите:

1. **Что показывает `node --version`?**
2. **Что показывает `npm --version`?**
3. **Что написано в терминале после `npm run dev`?**
4. **Какая ошибка в браузере (если есть)?**

---

**После успешного запуска вы увидите красивый лендинг Go Offer! 🎉**













