# 🚀 Быстрый старт: Автоматическое обновление

## Для Windows (PowerShell)

### Первая настройка (один раз):

1. **Запустите скрипт:**
   ```powershell
   .\scripts\update-from-template.ps1
   ```

2. **Введите URL оригинального репозитория шаблона:**
   - Например: `https://github.com/username/ai-first-workspace-template.git`
   - Скрипт сохранит этот URL для будущих обновлений

### Регулярные обновления:

Просто запустите:
```powershell
.\scripts\update-from-template.ps1
```

Скрипт автоматически:
- ✅ Сохранит ваши незакоммиченные изменения
- ✅ Получит обновления из шаблона
- ✅ Сольет изменения
- ✅ Восстановит ваши изменения

## Для автоматического обновления (GitHub Actions)

1. **Добавьте Secret в GitHub:**
   - Settings → Secrets and variables → Actions
   - New repository secret
   - Name: `UPSTREAM_TEMPLATE_URL`
   - Value: URL вашего шаблона

2. **Готово!** 
   - Обновления будут применяться автоматически каждую неделю
   - Или запустите вручную: Actions → "Auto Update from Template"

## Подробная инструкция

См. файл `AUTO-UPDATE-SETUP.md` для полной документации.




















